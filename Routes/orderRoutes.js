// const express = require('express');
// const router = express.Router();
// const orderController = require('../Controllers/orderController');
// const { protect } = require('../Middlewares/auth'); // if you use authentication

// // Create a new order (PhonePe or COD)
// router.post('/orders', protect, orderController.createOrder);

// // PhonePe payment callback (handle after payment)
// router.post('/payments/callback/:merchantTransactionId', orderController.handlePhonePeCallback);

// module.exports = router;
